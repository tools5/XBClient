{
  "name": "xbclient-electron",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "main": "bootstrap.mjs",
  "author": "PassClient",
  "homepage": "https://github.com/MoeclubM/XBClient",
  "description": "XBClient desktop client",
  "scripts": {
    "dev": "electron . dev",
    "start": "electron . build",
    "icons": "node build/generate-icon.mjs",
    "prepare-dist": "node build/prepare-dist.mjs",
    "build:backend": "cargo build --release --manifest-path backend/Cargo.toml && node build/check-wintun.mjs",
    "build:web": "pnpm --filter xbclient-web build",
    "build": "pnpm run icons && pnpm run build:web && pnpm run build:backend",
    "dist": "pnpm run prepare-dist && pnpm run build && electron-builder -p never --config build/electron-builder.generated.yml",
    "dist:win": "pnpm run prepare-dist && pnpm run build && electron-builder --win -p never --config build/electron-builder.generated.yml",
    "dist:linux": "pnpm run prepare-dist && pnpm run build && electron-builder --linux -p never --config build/electron-builder.generated.yml",
    "dist:linux:deb": "pnpm run prepare-dist && pnpm run build && electron-builder --linux deb -p never --config build/electron-builder.generated.yml"
  },
  "dependencies": {
    "auto-launch": "5.0.6",
    "electron-updater": "6.8.3"
  },
  "devDependencies": {
    "electron": "42.3.0",
    "electron-builder": "26.0.12",
    "pngjs": "7.0.0",
    "to-ico": "1.1.5"
  }
}

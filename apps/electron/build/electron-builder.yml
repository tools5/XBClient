appId: moe.telecom.xbclient
productName: XBClient
copyright: Copyright © PassClient
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
asar: true
npmRebuild: false
directories:
  output: dist
  buildResources: build
files:
  - bootstrap.mjs
  - main.mjs
  - preload.cjs
  - updater.mjs
  - package.json
extraResources:
  - from: build/icon.png
    to: icon.png
  - from: web/dist
    to: web/dist
  - from: backend/target/release
    to: bin
    filter:
      - xbclient-electron-backend*
      # Windows TUN 依赖 wintun.dll（tun2proxy build.rs 会复制到 target/release）。
      # 它必须与 backend 可执行文件同目录，否则打包版 TUN 创建虚拟网卡时
      # LoadLibrary 找不到 DLL，会话启动即静默死亡（表现为连接后立即断开/无流量）。
      - wintun.dll
  - from: ../../rust/aerion-core/assets/route
    to: route
  - from: resources/config
    to: config
    filter:
      - build-config.json
win:
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64
  icon: build/icon.ico
  executableName: XBClient
  requestedExecutionLevel: requireAdministrator
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  allowElevation: true
  createDesktopShortcut: always
  createStartMenuShortcut: true
  shortcutName: ${productName}
  runAfterFinish: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  differentialPackage: true
  deleteAppDataOnUninstall: false
portable:
  artifactName: ${productName}-${version}-portable-${os}-${arch}.${ext}
linux:
  target:
    - deb
  icon: build/icon.png
  category: Network
  executableName: XBClient
  executableArgs:
    - --enable-features=UseOzonePlatform
    - --ozone-platform-hint=auto
  desktop:
    entry:
      Name: XBClient
      Comment: XBClient proxy client
      Categories: Network;
      StartupWMClass: XBClient
  maintainer: PassClient
  synopsis: XBClient proxy client
  description: Desktop client for PassClient (Wayland-first on GNOME/KDE).
deb:
  depends:
    - gsettings-desktop-schemas
    - libgtk-3-0
    - libnotify4
    - libayatana-appindicator3-1 | libappindicator3-1
